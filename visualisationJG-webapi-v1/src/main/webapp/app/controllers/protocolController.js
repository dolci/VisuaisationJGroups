
// contrôleur
angular.module("visualjgroups")
  .controller('protocolCtrl', ['$scope', '$location',
        function ($scope, $location) {
 
      // modèle de la page
    //  var protocol = $scope.protocol;
    //  protocol.text = "[Modèle local dans page 1]";
    }]);
