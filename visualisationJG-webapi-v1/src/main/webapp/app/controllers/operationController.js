angular.module("visualjgroups")
    .controller('operationCtrl', ['$scope', '$location',
        function ($scope, $location) {

            // modèle de la page
            var operation = $scope.operation;
            graphe.text = "[Modèle local dans page 1]";
        }]);
